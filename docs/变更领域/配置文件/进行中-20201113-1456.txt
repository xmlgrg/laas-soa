配置文件
	开放数据
		增强cmdb
			为每列添加一个是否开放数据选项
	操作界面
		增强cmdb
			添加一个字段顺序栏, 展示数据时会按照顺序展示
			保存数据时校验reference_type的值, 如果为空则不校验, 看引用的对象的属性是否存在
			添加字段类型一栏, 默认为string, 可为list_string、path_string
		增强operate
			操作界面
				添加3层
					上层数据模型
					下一层数据树
					下一层数据模型

				嵌入到业务场景中时, 可以约束数据列表范围, 实现精准业务场景中最佳多数据模型效果
				如果上一层已经展示, 下一层数据模型层只有数据树和上一层的id, 那么这一层就会直接隐藏起来, 只是展示数据树
				如果没有数据列表(list, path)则不展示数据树
				每一层可以收缩起来, 数据模型和数据树可以收缩起来
			数据版本:
				文件:
					查看该文件历史版本数据
					对比某一历史版本
					从某一历史版本检出到当前
	配置文件特有业务:
		校验模板文件是否有问题
		发起合并到其他环境的审批
